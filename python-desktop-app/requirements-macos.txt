# macOS Requirements for Time Tracker Desktop Application
# Cross-platform dependencies
flask>=3.0.0
flask-cors>=4.0.0
pillow>=10.1.0
requests>=2.31.0
supabase>=2.0.0
python-dotenv>=1.0.0
psutil>=5.9.6
cryptography>=41.0.7
pynput>=1.7.6
keyring>=24.3.0

# macOS-specific dependencies
pyobjc-framework-Cocoa>=10.0
pyobjc-framework-Quartz>=10.0
pyobjc-core>=10.0

# Cross-platform notification support (replaces winotify on Windows)
plyer>=2.1.0

# System tray support (cross-platform)
pystray>=0.19.5

# Timezone support
tzlocal>=5.2

# SQLite support (usually built-in but included for completeness)
# sqlite3 is built into Python

# Optional: For building standalone apps
# pyinstaller>=6.3.0