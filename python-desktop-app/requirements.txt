flask==3.0.0
flask-cors==4.0.0
supabase==2.0.0
pystray==0.19.5
Pillow>=11.0.0
psutil==5.9.6
requests==2.31.0
python-dotenv==1.0.0
cryptography==41.0.7
pyinstaller>=6.16.0
pynput==1.7.6
keyring==25.2.1
tzlocal>=5.0
jaraco.text>=4.0.0
jaraco.functools>=4.0.0
jaraco.context>=6.0.0

# Cross-platform screenshot capture (REQUIRED for both Windows and Linux)
mss==9.0.1

# Windows-specific dependencies
pywin32==306; sys_platform == 'win32'
winotify==1.1.0; sys_platform == 'win32'

# Linux-specific dependencies
ewmh==0.1.6; sys_platform == 'linux'
python-xlib==0.33; sys_platform == 'linux'
# For Wayland portal with persistent permissions (persist_mode=2)
# Note: PyGObject (python3-gi) should be installed via system package manager:
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0
# dbus-python requires: sudo apt install libdbus-1-dev pkg-config python3-dev build-essential
dbus-python==1.3.2; sys_platform == 'linux'
